{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Only the GitPersona main window receives core application access.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
